import {FC} from 'react';
import Presentation from "../components/layout/Presentation";

const Settings: FC<{}> = ({}) => {
    return (
        <>
            <Presentation>
                Paramètres
            </Presentation>
            <main>
                <section>
                    <article>

                    </article>
                </section>
            </main>
        </>
    );
};

export default Settings;
